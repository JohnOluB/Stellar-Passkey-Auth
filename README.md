# Stellar-Passkey-Auth
An open-source authentication module that enables Stellar dApp users to sign transactions using passkeys or social login (Google, GitHub), abstracting raw key management entirely.
